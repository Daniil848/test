import { AppBar, Toolbar, IconButton, } from "@mui/material";


const Header = () => {
  return (
    <AppBar>
      <Toolbar>
        <IconButton>

        </IconButton>
      </Toolbar>
    </AppBar>
  );
};

export default Header;